import { View, Text, TouchableOpacity } from "react-native";

// Fetch brugerens rating array of iterer igennem dem og gem alle id'erne og ratings != fejlhåndtering ikke vis
// Gem i en useState array til at kunne læse igennem

export const CapsuleRating = ({navigation, route}) => {
    return (
        <View>
            <Text>CapsuleRating</Text>
        </View>
    )
}